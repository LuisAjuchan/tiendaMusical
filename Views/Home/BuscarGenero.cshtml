@model TiendaMusical.Models.Genres

@{
    ViewBag.Title = "BuscarGenero";
}

<h2>Genero: @Model.Name</h2>
<ul>
    @foreach (var album in Model.Albums)
    {
        <li>@album.Title</li>
    }
</ul>

